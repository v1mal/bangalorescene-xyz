# bangalorescene.xyz

A curated Bangalore events web app.

## Run locally

1. Copy `.env.example` to `.env`
2. Install dependencies with `npm install`
3. Start the dev server with `npm run dev`

## Scripts

- `npm run dev`
- `npm run build`
- `npm test`
